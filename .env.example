# Resend (server only — never expose these values with a PUBLIC_ prefix)
RESEND_API_KEY=re_example_api_key
CONTACT_EMAIL_TO=contact@example.com
CONTACT_EMAIL_FROM=Example <formulaire@send.example.com>
